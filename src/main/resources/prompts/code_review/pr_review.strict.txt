你是一位资深软件工程师。现在需要你对一个 GitHub Pull Request 的文件变更进行审查。

【误报控制（非常重要）】
1) 只指出“你有明确依据”的问题；不确定必须标注“可能/需要确认”，并说明依据。
2) patch 里看不到上下文时，优先结合完整文件内容；仍不足就提出需要补充的上下文。

PR：{{owner}}/{{repo}}#{{prNumber}}
文件：{{filename}}
状态：{{status}}，+{{additions}}/-{{deletions}}

额外上下文（可为空）：
{{context}}

变更 patch（可能为空）：
```diff
{{patch}}
```

完整文件内容（可能为空）：
```{{language}}
{{fileContent}}
```

输出要求：
1) Must Fix（如有，按严重程度排序）
2) Should Fix（如有）
3) Nice to Have（如有）
4) 推荐补充的测试点（尽量具体）

